apiVersion: v1
kind: Service
metadata:
  name: pragna-api
  namespace: pragna
spec:
  selector:
    app: pragna-api
    slot: blue   # active slot — patched to 'green' on each blue-green switch
  ports:
    - port: 8000
      targetPort: 8000